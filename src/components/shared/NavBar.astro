---
const links = [
    { name: 'Listado', href: '/' },
    { name: 'Paginado', href: '/pokemons/1' },
    { name: 'Favorito', href: '/favorites' },
]
---


<!-- component -->
<nav class="shadow bg-slate-900">
    <div class="container flex items-center justify-center p-6 mx-auto text-gray-600 capitalize dark:text-gray-300">
     {
        links.map(({href,name}) => (
        <a href={href}
        class="text-gray-600 dark:text-gray-600 border-b-2 border-blue-500 mx-1.5 sm:mx-6">
        {name}
        </a>
     ))
     } 
          
    </div>
</nav>